# Database
DATABASE_URL=postgresql://galaos:galaos_dev_password@localhost:5432/galaos

# Redis
REDIS_URL=redis://localhost:6379

# JWT Secret (change in production!)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Encryption Key for API keys (REQUIRED - generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
ENCRYPTION_KEY=generate-a-32-byte-hex-key-using-command-above

# AI Providers (OPTIONAL - can add via Settings UI instead)
ANTHROPIC_API_KEY=your-anthropic-api-key-here
OPENAI_API_KEY=your-openai-api-key-here

# Integration Providers (OPTIONAL - can add via Settings UI instead)
BUFFER_ACCESS_TOKEN=your-buffer-token-here
SENDGRID_API_KEY=your-sendgrid-key-here

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:3001/trpc

# Server
PORT=3001
NODE_ENV=development
